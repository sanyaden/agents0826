# 1.x прибрав temperature з messages.create(). Код працює на обох
# мажорах, але тестуємо на 0.x — саме його ставить ця вимога:
anthropic>=0.40,<1
python-dotenv>=1.0.0

sentence-transformers>=3.0
qdrant-client>=1.12        # векторна БД: вбудований :memory: або сервер

# фреймворки:
langchain>=1.3
langgraph>=1.2

# MCP-сервер. У SDK 2.0 клас FastMCP перейменували на MCPServer — наші
# файли підтримують обидві версії, але 2.x бажаніший: лише він узгоджує
# ревізію протоколу 2026-07-28, про яку йдеться на слайдах.
mcp[cli]>=2.1

# A2A (a2a_server.py + a2a_client.py). Саме 1.x — у 0.x інші типи й методи:
a2a-sdk>=1.1
uvicorn>=0.30
fastapi>=0.115
httpx>=0.27
